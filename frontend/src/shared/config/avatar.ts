/**
 * Аватар, когда пользователь свой не загрузил.
 *
 * В контракте поле `avatar` приходит как `null` — и приходит так всегда:
 * ручки, которая бы его записала, нет вовсе (матрица паритета, п. 21 —
 * «аватар сохранять некуда, поле только читается»). Пустой `src` дал бы
 * битую картинку, поэтому заглушка нужна каждому месту, где показывают
 * пользователя: меню профиля в шапке и карточке личного кабинета.
 *
 * Отдельный модуль, а не константа в одном из них: путь к файлу,
 * продублированный в двух местах, разъедется при первой же чистке
 * `public/assets` — а исчезнувшую картинку заметит не сборка, а посетитель.
 */
export const DEFAULT_AVATAR_URL = '/assets/images/avatars/default-user.png';
