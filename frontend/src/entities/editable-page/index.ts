/**
 * Публичный вход в сущность «редактируемый раздел»: снаружи берут отсюда,
 * а не из `api` напрямую.
 *
 * Потребителей два: главная (F-17), которой нужны два блока из тринадцати,
 * и страница раздела (F-23) — девять разделов по своим адресам меню.
 * Обоим хватает одного `editablePageQuery`: чтение и ключи кэша готовы,
 * F-23 не дописал сюда ни строки.
 *
 * Ни `editablePageKeys`, ни голый `fetchEditablePage` наружу не выходят,
 * хотя заведены: ключи некому инвалидировать (правки нет), а запрос мимо
 * кэша некому делать — оба блока главной читаются через Query. Публичным
 * объявляется то, чем пользуются, иначе первое же неверное применение
 * приезжает вместе с экспортом; тем же правилом живёт `entities/news`.
 */
export { editablePageQuery } from './api/editablePageQueries';
