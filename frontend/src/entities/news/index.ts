/**
 * Публичный вход в сущность «новость»: снаружи берут отсюда,
 * а не из `api/ui/lib` напрямую.
 *
 * Сущность знает только чтение — списки и одну запись. Создание, правку
 * и удаление принесёт фича админки (блок 4 бэклога); лежать они будут
 * там, а не здесь: `entities` описывает предметную область, действия
 * пользователя — дело `features`.
 *
 * `newsKeys` наружу не выходит, хотя иерархия ключей заведена: сегодня
 * инвалидировать кэш новостей некому — правки нет. Экспортировать его
 * заранее значит объявить публичным то, чем никто не пользуется, и первым
 * же неверным применением обзавестись. Понадобится админке — добавим строку.
 */
export { newsListQuery, newsItemQuery } from './api/newsQueries';
export { findCachedNews } from './lib/cachedNews';
export { postTypeLabel, authorLabel } from './lib/newsPresenters';
export { NewsCard } from './ui/NewsCard';
