/**
 * Публичный вход в сущность: снаружи берут отсюда, а не из `api`
 * напрямую.
 *
 * Сущность знает только чтение справочника — того минимума учётной
 * записи (`id` и ФИО), который открыт не одному админу. Список учёток
 * целиком, создание и блокировка приедут с F-46 и лягут в фичу:
 * `entities` описывает предметную область, действия пользователя — дело
 * `features`.
 *
 * Своего `ui` у сущности нет: учётка показывается строкой выпадающего
 * списка в форме карточки ППС — то же решение, что у дисциплины
 * (`entities/subject`).
 */
export { fetchUserDirectory } from './api/userApi';
export { userDirectoryQuery, userKeys } from './api/userQueries';
