/**
 * Публичный вход в фичу: снаружи берут отсюда, а не из `model`/`ui`
 * напрямую.
 *
 * Наружу выходят двое — окно формы целиком и хук удаления. Форма
 * и её хук остаются внутренними: собирать их по отдельности незачем,
 * а у удаления окно подтверждения общее (`shared/ui/ConfirmDialog`),
 * и держать его открытым решает страница.
 */
export { NewsDialog } from './NewsDialog';
export { useDeleteNews } from './model/useDeleteNews';
