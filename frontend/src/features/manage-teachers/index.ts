/**
 * Публичный вход в фичу: снаружи берут отсюда, а не из `model`/`ui`
 * напрямую.
 *
 * Одна фича на двух действующих лиц: преподаватель правит свою карточку
 * из личного кабинета (F-16), модератор заводит, правит и удаляет любые
 * из админки (F-44). Разными фичами они были ровно до тех пор, пока
 * вторая не появилась: карточка у них одна и та же, а значит одни и те же
 * восемнадцать полей, одни и те же проверки — те, что стоят
 * в `TeacherRequestDto`, — и одно и то же превращение `null` контракта
 * в пустую строку формы. Расползись это по двум фичам, и первая же правка
 * DTO чинилась бы в двух местах, причём про второе вспомнили бы после
 * отказа сервера.
 *
 * Различаются они тем, что действительно различается: ручкой, набором
 * полей сверх карточки и тем, кто открывает форму. Это разные хуки
 * и разные страницы, а не разные фичи.
 *
 * Фича отдельная от сущности `teacher` по правилу «сущность знает только
 * чтение». Публичные страницы ППС (F-21) читают сущность и о фиче
 * не знают.
 */
export { useMyTeacherCardForm } from './model/useMyTeacherCardForm';
export { useTeacherAdminForm } from './model/useTeacherAdminForm';
export { useDeleteTeacher } from './model/useDeleteTeacher';
export { TeacherCardForm } from './ui/TeacherCardForm';
export { AccountField } from './ui/AccountField';
export { SubjectsField } from './ui/SubjectsField';
