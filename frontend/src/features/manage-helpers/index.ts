/**
 * Публичный вход в фичу: снаружи берут отсюда, а не из `model`/`ui`
 * напрямую.
 *
 * Наружу выходят двое — окно формы целиком и хук удаления. Форма и её
 * хук остаются внутренними: собирать их по отдельности незачем,
 * а у удаления окно подтверждения общее (`shared/ui/ConfirmDialog`),
 * и держать его открытым решает страница.
 *
 * Своя фича, а не часть `manage-teachers`, хотя обе про сотрудников
 * кафедры: у УВП другой DTO, другие ручки и вчетверо меньше полей.
 * Общего у них ровно четыре имени поля — и это совпадение сегодняшних
 * DTO, а не одна сущность.
 */
export { HelperDialog } from './HelperDialog';
export { useDeleteHelper } from './model/useDeleteHelper';
