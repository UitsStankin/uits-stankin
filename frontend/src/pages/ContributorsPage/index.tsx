import { CONTRIBUTORS } from './lib/contributors';
import { ContributorGrid } from './ui/ContributorGrid';

/**
 * Благодарности тем, кто делал портал кафедры. Сборка: содержимое —
 * из данных в `lib/`, разметка — из чистых компонентов. Состояния нет
 * вовсе, поэтому нет и `model/`.
 *
 * Страница статическая: ручки у неё нет и не будет. Проверено при закрытии
 * F-23 — в `editable_pages` старого портала благодарностей не было, это
 * ровно та вёрстка руками, о которой предупреждает блок 3 бэклога.
 * И, в отличие от истории кафедры, редактируемой она не станет и потом:
 * список не про кафедру, а про работу над сайтом.
 *
 * ### Что поправлено по дороге
 *
 * **Заголовок.** В оригинале страница начиналась с `h3` — «Выражаем
 * огромную благодарность всем, кто принимал участие в создании портала
 * кафедры», — и другого заголовка на ней не было. Для читающего с экрана
 * это страница, начинающаяся с третьего уровня: `h1` и `h2` пропущены,
 * и по заголовкам к ней не перейти. Здесь `h1` называется так же, как
 * пункт меню, — «Благодарности», как названы «Контакты» в F-32, — а сама
 * фраза оригинала осталась подписью под ним. Текст не потерян, порядок
 * уровней восстановлен.
 *
 * **Карусель стала сеткой** — разбор в `ui/ContributorGrid`.
 *
 * **Фотографии пережаты** — 2.7 МБ на десять снимков стало 268 КБ,
 * это долг D-F5, закрытый здесь же: он и был записан «вместе с F-33».
 */
export default function ContributorsPage() {
  return (
    <div className="mx-auto flex w-full max-w-6xl flex-col gap-gutter">
      <header className="flex flex-col gap-2">
        <h1 className="text-h4 text-text-heading">Благодарности</h1>

        <p className="text-base text-text-muted">
          Выражаем огромную благодарность всем, кто принимал участие в создании портала кафедры
        </p>
      </header>

      <ContributorGrid contributors={CONTRIBUTORS} />
    </div>
  );
}
