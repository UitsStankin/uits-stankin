import type { PostType } from '@shared/types';

interface FeedCopy {
  /** Что диктор говорит вместо серых прямоугольников скелета. */
  loading: string;
  errorTitle: string;
  emptyTitle: string;
  emptyDescription: string;
}

/**
 * Подписи состояний ленты — по разделу.
 *
 * Русский язык не даёт собрать их шаблоном: «Новостей пока нет» и «Объявлений
 * пока нет» отличаются не подставленным словом, а падежом и родом. Поэтому
 * таблица, а не склейка строк.
 *
 * Таблица своя, а не общая с секциями главной, хотя четыре строки совпадают
 * дословно. Это тот же размен, что записан у второго запроса в
 * `pages/HomePage/model/useHomeContent.ts`: общий словарь на два места должен
 * был бы знать и о ленте с пагинацией, и о витрине со ссылкой «ко всем»,
 * — абстракция вышла бы дороже совпадения.
 *
 * Заголовка раздела здесь нет намеренно: `h1` рисует страница. Виджет —
 * лента, а не страница целиком, и знать, как называется раздел, ему незачем.
 */
export const FEED_COPY: Record<PostType, FeedCopy> = {
  news: {
    loading: 'Загрузка новостей',
    errorTitle: 'Не удалось загрузить новости',
    emptyTitle: 'Новостей пока нет',
    emptyDescription: 'Как только на кафедре что-то появится, это будет здесь.',
  },
  announcements: {
    loading: 'Загрузка объявлений',
    errorTitle: 'Не удалось загрузить объявления',
    emptyTitle: 'Объявлений пока нет',
    emptyDescription: 'Как только на кафедре что-то объявят, это будет здесь.',
  },
};
