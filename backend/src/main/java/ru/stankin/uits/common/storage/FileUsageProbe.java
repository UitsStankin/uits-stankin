package ru.stankin.uits.common.storage;

/**
 * Знает, ссылается ли хоть одна сущность модуля на файл с таким ключом.
 *
 * <p>Интерфейс объявлен рядом с хранилищем, а реализации живут в модулях: спрашивать
 * чужие репозитории напрямую хранилищу нельзя, модули друг о друге не знают.
 */
public interface FileUsageProbe {

    boolean uses(String key);
}
