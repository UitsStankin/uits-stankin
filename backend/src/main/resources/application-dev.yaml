spring:
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

# Раскомментировать при разборе того, как запрос проходит цепочку фильтров
# Spring Security. Уровень шумный, поэтому живёт только в dev-профиле.
#logging:
#  level:
#    org.springframework.security: DEBUG
